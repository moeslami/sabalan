import blogPages from './blog-pages.json';
import contentPages from './content-pages.json';

const routes: {blogPages: any, contentPages: any} = {
  blogPages,
  contentPages
};

export default routes;
